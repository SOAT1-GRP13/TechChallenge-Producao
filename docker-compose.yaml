version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tech-challenge-soat1-grp13-producao:latest
    restart: on-failure
    ports:
      - "80:80"
    depends_on:
      - postgres-db

  postgres-db:  
    hostname: postgres-compose
    image: postgres
    environment:
      POSTGRES_PASSWORD: "S3nh@L0c@lP40ducao"
      POSTGRES_USER: fiap
      POSTGRES_DB: techChallengeProducao
    ports:
      - "15432:5432"
    volumes:
      - tech-challenge-soat1-grp13-producao-db-data:/var/lib/postgresql/data

volumes:
  tech-challenge-soat1-grp13-producao-db-data: